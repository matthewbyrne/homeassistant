climate:
  - platform: generic_thermostat
    name: Livingroom
    heater: switch.living_room_cold
    target_sensor: sensor.livingroom__temperature
    away_temp: 12
    cold_tolerance: 0.1
  - platform: generic_thermostat
    name: Fias bedroom
    heater: switch.fias_bedroom_cold
    target_sensor: sensor.fias_bedroom__temperature
    away_temp: 12
    cold_tolerance: 0.1
  - platform: generic_thermostat
    name: Master Bedroom
    heater: switch.master_bedroom_cold
    target_sensor: sensor.master_bedroom__temperature
    away_temp: 12
    cold_tolerance: 0.1
